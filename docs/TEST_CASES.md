# TEST_CASES
TC01 R01: Electron isolated window starts against production output; Windows package exists; mac build CI is configured, native verification pending Mac.
TC02 R02/R11: reject ZIP traversal, symlinks, unsupported extensions, excessive count/size and corrupted archive; retain ordinary assets and identify entry pages.
TC03 R04: temp Git repository refs/tags, dirty detection, branch validation; create branch at frozen commit or switch existing without data loss; reject moved HEAD/stale plan.
TC04 R03/R05: structured plan parser rejects invalid/empty output; state guards prevent develop without plan, duplicate runners and accept before tests/start; cancellation terminates child process tree.
TC05 R06: successful/failed/missing script and skill outcomes remain distinct; manual intervention begins actual checks, failed test cannot deliver.
TC06 R08/R09: persisted restart/atomic save, interrupted runs marked stopped; secrets encrypted and omitted from bootstrap/logs/export; validation rejects malformed settings/paths/URLs.
TC07 R07: readiness timeout/failure keeps failed state, ready target opens approved HTTP loopback URL, acceptance only review state.
TC08 R10: theme swatches/custom color save and reset, primary button white contrast >=4.5, readable logs.
TC09 R02/R08: intake switch, attachments, filter/history/details and plan/test preview work; empty/loading/error states; bounded logs and 20-row pagination.
TC10 R11: imported page has no preload/node/IPC; IPC rejects foreign frame; external navigation is restricted.
